# 계획.md — 말년휴가 대장 프로젝트

## 목표
전역까지 남은 휴가를 관리하는 개인용 웹앱.
지금은 로컬 HTML 파일 하나. 최종 목표는 배포 후 홈 화면에서 바로 보이는 형태(위젯류)까지 가는 것.

```
로컬 단일 파일  →  배포(URL 확보)  →  UI/UX 리디자인  →  홈 화면 위젯 연동
   Phase 1           Phase 2              Phase 3            Phase 4

지금 위치: Phase 3 구현 완료 (2026-09-08) / Phase 2 배포는 사용자 계정 연동 대기
```

---

## 로드맵

**Phase 1 — 로컬 단일 파일 (완료)**
- 대장: 항목별로 숫자를 편집하면 합계가 자동 갱신되는 휴가 집계표
- 달력: 확정된 4회차 일정 캘린더 뷰
- D-Day 게이지: 10/19 첫 휴가까지 자동 계산, 1분마다 갱신
- 결과물: `말년휴가_대장.html` (단일 파일, 외부 의존성 없음)

**Phase 2 — 배포: Cloudflare Pages (방식 확정, 미착수)**
- 목표: 실제 URL 확보. Phase 4(위젯 연동)의 선행 조건 — 로컬 파일은 웹 위젯 앱에 못 붙는다
- **확정 방식: Cloudflare Pages** (2026-09-08 사용자 결정)
  - 선정 이유: private 저장소인 채로 배포된다. GitHub Pages는 무료로 쓰려면 저장소가
    public이어야 해서 휴가 일정이 공개되는데, Cloudflare Pages는 그 제약이 없다.
    push = 자동 재배포, HTTPS·커스텀 도메인 기본, Phase 4가 계속 때릴 URL로 안정적이다.
  - 빌드 설정: 빌드 명령 없음 / 출력 디렉터리 = 저장소 루트 (단일 HTML을 그대로 서빙)
- **설정 절차** (1~3은 사용자가 직접, 그 뒤로는 push만 하면 자동 배포)
  1. dash.cloudflare.com 가입 (무료 플랜으로 충분)
  2. Workers & Pages → Create → Pages → Connect to Git → GitHub 연결
     (권한은 "Only select repositories"로 `limfighter/limen`만 주면 된다)
  3. 빌드 설정
     ```
     Production branch    claude/new-session-m5x7dr   (현재 저장소의 기본 브랜치)
     Framework preset     None
     Build command        mkdir -p dist && cp *.html dist/index.html
     Output directory     dist
     ```
     - 저장소에 HTML이 이 파일 하나뿐이라 `*.html`로 잡았다.
       HTML을 더 추가하게 되면 파일명을 명시하도록 바꿔야 한다
     - `dist`로 따로 빼는 이유: 루트를 그대로 배포하면 CLAUDE.md·계획.md까지
       같이 공개된다. 내보낼 파일만 담아서 올린다
     - 루트 URL로 열려야 하므로 배포 시 파일명을 index.html로 바꾼다.
       저장소 안의 원본 파일명(`말년휴가_대장.html`)은 그대로 두므로
       "다운로드해서 쓰는 단일 HTML" 규약과 충돌하지 않는다
  4. Save and Deploy → `<프로젝트명>.pages.dev` URL 발급 (Phase 4의 입력값)
- **접근 제어 주의**: pages.dev URL은 링크만 알면 누구나 열 수 있고 주소도 추측 가능하다.
  Cloudflare Access로 이메일 인증을 걸 수 있지만, 인증을 걸면 Phase 4의 위젯이
  페이지를 못 읽는다. 위젯 연동을 할 거면 인증은 걸지 않는 쪽이다 (선택 필요)
- 배포 후에도 L2 규약(외부 리소스 없는 자립형 단일 파일)은 그대로 유지한다.
  그래야 URL 접근과 로컬 `file://` 열람이 동시에 된다

**Phase 3 — 기능 추가 + 리디자인 (구현 완료)**
- 2026-09-08 사용자가 아래 6개 기능을 선택. 세부 스펙은 "Phase 3 작업 계획" 절 참조
- 톤앤매너(웜톤 아이보리 캔버스, clay 단일 액센트, 세리프/산세리프 이중 조합)는
  업로드받은 디자인 토큰 기준이라 유지

**Phase 4 — 위젯 연동 (예정)**
- 목표: 삼성폰 홈 화면에 D-Day 게이지가 앱을 열지 않고도 바로 보이게
- 제약: 안드로이드 네이티브 위젯은 별도 앱 개발 필요 — 웹페이지로는 원천적으로 불가
- 대안: KWGT 등 "웹 위젯" 앱에 Phase 2에서 확보한 URL을 연결
- Phase 3의 "홈 화면 추가 최적화"가 여기 사전작업에 해당

---

## 확정된 사실 (재검증·재설계 대상 아님)

```
총 휴가       46일
  연가 23 / 운전 4 / 뜀걸음 2 / 야근 3 / 사격특급 1 / 중대장 1 /
  모범용사(앱개발) 3 / 드림 3 / 종교 3 / 시설방문 1 / 구직 2

확정 일정     4회차 (2026-09-08 중대장 합의 완료)
  1회차  10/19(월) ~ 10/29(목)   11일
  2회차  11/2(월)  ~ 11/12(목)   11일
  3회차  11/15(일) ~ 11/26(목)   12일
  4회차  11/30(월) ~ 12/11(금)   12일

입대일        2025-06-17(화)   ← 전역 게이지 기준점 (2026-09-08 사용자 확정)
전역일        12/16(수)         입대~전역 총 548일
전투휴무증    보유 13장, 확정 일정에서는 0장 사용 (전부 잔여)
훈련 5구간    전부 커버 확인됨
```

**이 확정 일정은 사용자가 실제 부대에서 합의를 보고 온 결과다.**
날짜·요일이 이상해 보여도(예: 3회차가 일요일 시작) 임의로 "정정"하지 말 것.
확인이 필요하면 지적은 하되, 사용자가 "합의 보고 왔다"고 하면 그대로 확정으로 받아들인다.

> 저장소 커밋 시점(2026-09-08)에 `말년휴가_대장.html`의 `leaves` 원본 데이터를 세어
> 위 확정값과 대조 완료: 총 46일 / 4회차(11·11·12·12, 전 구간 연속) /
> 전투휴무증 0장 사용 / 훈련커버 5-5. 대장 기본값 합계도 46일로 일치.

---

## Phase 3 작업 계획 (2026-09-08 확정 · 구현 완료)

### 핵심 기능 4개

**① 대장 ↔ 달력 연동 · 잔여 휴가**
```
문제: 지금은 대장(보유 46일)과 달력(소진 46일)이 서로를 전혀 모른다
구현: 오늘 날짜 기준으로 leaves 원본에서 "이미 쓴 일수 / 남은 예정 일수"를 계산하고,
      대장 히어로 카드에 보유·사용·잔여를 같이 표시
불일치 처리: 대장 총합은 사용자가 편집할 수 있어서 달력 46일과 어긋날 수 있다.
      어긋나면 배지로 "지적만" 하고 어느 쪽도 자동 수정하지 않는다 (L0)
L1: 세 수치 전부 렌더 시점에 leaves + 대장 state에서 계산. 하드코딩 금지
```

**② D-Day 자동 전환 · 전역 게이지**
```
문제: 현재 TARGET이 10/19로 하드코딩돼 있어 10/19가 지나면 화면이 무의미해진다
구현: 회차 시작·종료일을 leaves에서 파생시키고, 오늘 위치에 따라 3가지 상태로 자동 전환
      - 휴가 전   → 다음 회차 시작까지 D-n
      - 휴가 중   → 복귀까지 D-n
      - 전부 종료 → 전역(12/16)까지 D-n
      여기에 전역일 기준 게이지를 별도로 추가
1분 간격 갱신은 유지
L1: 회차 날짜를 새로 적지 말고 반드시 leaves에서 파생시킬 것
```

**③ 달력에 오늘 표시 · 지난날 구분**
```
구현: 오늘 셀에 액센트 테두리, 이미 지나간 날짜는 톤을 낮춰 구분
L2: 새 색을 만들지 말고 기존 토큰(accent/muted/panel) 안에서 처리
```

**④ 전투휴무증 13장 관리**
```
구현: 실제로 쓴 날짜를 기록하면 잔여 장수가 자동으로 줄어드는 방식
L0 주의: 확정 일정의 plans.confirmed.combatSet(=0장)은 확정 데이터이므로 건드리지 않는다.
      사용자 사용 기록은 별도 레이어로 얹고, 잔여는 13 - (기록된 장수)로 계산
저장: 대장과 다른 별도 localStorage 키를 쓴다 (대장 저장값을 오염시키지 않기 위해)
```

### 보조 항목 2개

**⑤ 홈 화면 추가 최적화 (Phase 4 사전작업)**
```
구현: 홈 화면에 추가했을 때 아이콘·이름·테마색이 제대로 나오게 하고,
      위젯처럼 한 화면에 요약만 보이는 컴팩트 뷰를 준비
제약: L2에 따라 외부 파일 없이 단일 HTML 안에서 해결해야 한다
      (아이콘·manifest를 data: URI로 인라인)
```

**⑥ 다크모드 수동 토글**
```
현재: CSS에 data-theme 처리가 이미 들어가 있고 폰 설정을 따라가기만 한다
구현: 페이지 안에서 밝게/어둡게 전환하는 버튼 추가, 선택값은 별도 키로 저장
```

### 선택하지 않은 항목
```
데이터 백업(JSON 내보내기/가져오기) — 이번 범위에서 제외.
  다만 localStorage는 브라우저 캐시를 지우면 날아가므로, 필요해지면 다시 논의
```

### 저장소(localStorage) 정책
```
대장 키 leave-ledger-v2 는 그대로 유지한다.
  → 이번 작업에서 대장의 기본 항목 구성·수량을 바꾸지 않으므로 버전 승격 불필요 (L0)
전투휴무증 기록·테마 선택은 각각 새 키를 신설한다
  → 기존 저장값과 충돌하지 않고, 하나가 깨져도 나머지가 살아남는다
읽기·쓰기는 전부 try/catch (L2)
```

### 작업 순서 / 진행 상태
```
S1  D-Day 엔진을 leaves 파생으로 교체 (②)          완료
S2  대장 ↔ 달력 연동, 잔여 휴가 표시 (①)            완료
S3  달력 오늘/지난날 표시 (③)                       완료
S4  전투휴무증 기록 UI (④)                          완료
S5  다크모드 토글 (⑥)                               완료
S6  홈 화면 최적화 + 컴팩트 요약 뷰 (⑤)             완료
S7  Cloudflare Pages 배포 (Phase 2)                 대기 — 사용자 계정·저장소 연동 필요
```

### 구현하면서 확정된 사항
```
전역 게이지 기준점  입대일 2025-06-17 (사용자 확정). 복무 총 548일 기준으로
                  "복무 n일차 / 총 548일 · xx%"를 계산한다
컴팩트 뷰 진입     ?view=compact URL 파라미터 (추천안 채택).
                  Phase 4에서 위젯에 URL만 꽂으면 되도록 한 선택.
                  파라미터가 없으면 평소 화면, 있으면 요약 카드만 표시
달력 표시 월       기존에는 10~12월 고정이라 오늘(9/8)이 화면에 아예 없었고,
                  그래서 "오늘 표시"와 9월 전투휴무증 기록이 불가능했다.
                  → 일정이 있는 달 + 오늘이 속한 달을 자동으로 표시하도록 변경.
                  10~12월 고정으로 되돌리고 싶으면 displayMonths()만 지우면 된다
회차 표시 색       하드코딩 hex를 --round-N-bg/fg 토큰으로 옮기고 다크 팔레트를 추가.
                  기존에는 다크모드에서 회차 색이 밝은 톤 그대로 떠 있었다 (L2)
다크 팔레트 문법   CSS 중첩(@media 안쪽 중첩)을 표준 문법으로 폈다.
                  구형 삼성 인터넷에서 다크모드가 적용되지 않을 여지를 없애기 위함
```

### 렌더 검증 기록 (2026-09-08, jsdom)
```
날짜를 바꿔가며 실제 렌더 결과를 재현해서 확인했다 (L1 ②).
  D-Day 3상태 전환   9/8 → "1회차 휴가까지 D-41" / 10/19 → "1회차 복귀까지 D-11"
                    10/30 → "2회차 휴가까지 D-3" / 12/12 → "전역까지 D-4"
  전역 게이지        입대일 0.0% · 9/8 81.9%(449일차) · 전역일 100.0%(548일차)
  대장↔달력 연동     9/8 사용 0·잔여 46 → 10/25 사용 7·잔여 39 → 12/12 사용 46·잔여 0
  불일치 배지        대장을 47일로 늘리면 "달력 배정 46일과 1일 차이" 배지 노출,
                    데이터는 어느 쪽도 자동 수정되지 않음 (L0)
  전투휴무증         기록/삭제/13장 상한 경고 동작, 별도 키에만 저장됨
  요약 4수치         46일 · 4회차 · 0장 · 5/5 — 확정된 사실과 일치
  플랜 카드          .active가 코드로 붙어 실제로 렌더됨 (과거 사고 재발 없음)
  달력 셀 인라인 hex  0건 (전부 토큰 참조)
```

### 검증 규약
```
UI를 고칠 때마다 코드만 읽고 판단하지 말고 실제 렌더링 결과를 재현해서 확인한다 (L1 ②).
요약 수치는 매번 leaves 원본에서 다시 세어 확정값(46일/4회차/0장/5구간)과 대조한다 (L1 ①).
```

---

## 미확정 / 보류

```
헌혈(1일)         — 받을 가능성 낮아 계속 제외
지게차 자격증(2일) — 별도 시도 중, 미확정. 확보되면 총합 재계산 필요

Cloudflare 계정·저장소 연동 — 사용자가 직접 수행해야 하는 단계, 아직 미완료.
                   이것만 끝나면 Phase 2는 push 만으로 완료된다
1회차 게이지 기준점 8/31 — 첫 휴가 게이지의 시작점은 기존 값(8/31)을 그대로 뒀다.
                   2회차부터는 직전 회차 복귀일이 자동으로 기준점이 된다
데이터 백업(JSON)  — 이번 범위에서 제외. localStorage가 날아갈 위험은 남아 있다
```
