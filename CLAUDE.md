# CLAUDE.md — 말년휴가 대장 작업 규칙
이 프로젝트에서 작업할 때 세션 시작 시 이 파일과 `계획.md`를 먼저 읽는다.
애매하면 "알아서 판단"으로 우회하지 말고 사용자에게 확인한다.

---

## 프로젝트 목표
```
전역까지 남은 휴가를 관리하는 개인용 웹앱 하나를 만드는 것.
지금은 로컬 HTML 파일 하나(대장+달력+D-Day)이고,
최종적으로는 배포 → 홈 화면 위젯 연동까지 간다.

현재 위치: Phase 1 완료 (로컬 단일 파일)
자세한 로드맵·확정 사실은 계획.md 참조
```

---

## 0. 세션 시작 시 필수 절차
```
1. 계획.md — 현재 어느 Phase인지, 뭐가 확정이고 뭐가 미확정인지 확인
2. 계획.md의 "확정된 사실" 절대 재검증·재설계하지 말 것
   (날짜·일수가 이상해 보여도, 사용자가 "합의 보고 왔다"고 했으면 확정이다)
3. 이 파일의 금지 규칙 확인 — 특히 L1 계산 무결
4. 애매한 부분(기능 범위, 디자인 디테일 등)은 임의로 정하지 말고 먼저 질문할 것
   → 실제 사례: "44일 플랜 쓰고 하나 붙이면 되겠네"라는 말만 듣고
     마지막 회차를 임의로 하루 늘려 45일 플랜을 만들었으나,
     사용자가 이후 완전히 다른 4회차 46일 안으로 확정하며 폐기됨.
     방법이 여러 개일 때(마지막 회차 연장 / 전투휴무증 전환 등)는
     하나를 정하지 말고 옵션을 보여줄 것
```

---

## 1. 절대 금지

### L0 — 데이터 무결
```
[금지][L0] 사용자가 확정했다고 말한 휴가 일정·일수를 임의로 수정하는 것
       → 계획.md "확정된 사실" 섹션이 원본. 의심되면 지적만 하고 사용자 판단을 기다린다
       → 실제 사례: 3회차가 일요일(11/15) 시작이라 이상해 보였지만
         "오늘 합의 보고 왔다"는 답을 받은 즉시 정정 시도를 멈추고 확정 처리함

[금지][L0] 기본값(항목 구성, 수량)을 바꾸면서 기존 localStorage 저장값과의
           충돌을 확인하지 않는 것
       → 기본값이 바뀌면 STORE_KEY 버전을 올릴 것 (v1 → v2 등)
       → 안 올리면 사용자가 예전에 저장해둔 값이 새 기본값을 덮어써서
         "숫자를 고쳤는데 왜 반영이 안 되냐"는 혼란이 생긴다
```

### L1 — 계산 무결
```
[금지][L1] 요약 수치(총소진일·회차·전투휴무증 장수·훈련커버)를
           원본 일정 데이터와 별개로 손으로 다시 입력하는 것
       → 반드시 leaves/combatSet 원본 데이터에서 자동 계산할 것
       → 실제 사고 (이 프로젝트 최초 버전): 39일 A/B 안이 전투휴무증
         "6장"이라 표기돼 있었으나 실제 날짜를 세어보니 9장이었고,
         49일 안도 "5장" 표기에 실제는 6장이었다.
         원인은 요약 카드 숫자를 달력 데이터와 별개로 수기 입력했기 때문.
         수정: 총소진/회차/전투휴무증/훈련커버 4개 수치 전부
         화면 렌더링 시점에 데이터에서 계산하도록 바꾸고,
         하드코딩된 필드(days/combat/rounds)는 아예 제거함
       → 새 플랜을 추가할 때도 이 원칙을 따를 것.
         스케줄 문자열(schedule)은 사람이 읽는 설명일 뿐이고,
         실제 판정 기준은 leaves 객체 하나뿐이다

[금지][L1] 여러 개의 "안(플랜)"을 화면에 만들면서 최초 활성 상태를
           명시적으로 지정하지 않는 것
       → 탭/카드를 JS로 동적 생성하는 구조에서는 첫 번째 항목에
         .active 클래스를 코드로 직접 붙여야 한다
       → 실제 사고: 확정 플랜 1개로 축소하면서 탭 바를 숨겼는데,
         정작 플랜 카드 자체에 active 클래스를 안 붙여서
         화면에 아무것도 안 보이는 상태로 나갈 뻔함.
         jsdom으로 실제 렌더링을 재현해서 발견·수정함
       → 규약: UI를 고칠 때마다 "코드가 실제로 그린 결과"를
         (가능하면 jsdom 등으로) 재현해서 확인하고, 코드만 읽고 판단하지 말 것
```

### L2 — 구현 규약
```
[규약][L2] 색상은 항상 CSS 변수(디자인 토큰)로 참조하고 hex를 직접 박지 말 것
       → 사용자가 제공한 토큰(ivory/oat/manilla/clay 등)이 유일한 팔레트다
       → 캘린더처럼 카테고리 구분이 필요한 곳도 임의의 원색(파랑/보라 등)
         대신 토큰 팔레트 안에서 톤을 만들어 쓸 것

[규약][L2] published 페이지가 아닌 "다운로드해서 쓰는 단일 HTML"이 기본 형태다
       → 외부 리소스(폰트 CDN, 이미지 URL 등) 없이 완전히 자립적으로 만들 것
       → 이유: 삼성 인터넷으로 로컬 file:// 열람 + 홈 화면 추가까지 고려해야 함

[규약][L2] localStorage 읽기/쓰기는 항상 try/catch로 감쌀 것
       → 실패해도 페이지가 죽지 않고 메모리 상태로라도 동작해야 함
```

---

## 2. 이 프로젝트에서 반복된 실패 패턴

```
① "요약 수치를 데이터와 따로 관리하다 어긋났다"
   39일 A/B 전투휴무증 6장→실제 9장 / 49일 5장→실제 6장
   → 원인은 하드코딩. 항상 원본 데이터에서 계산할 것 (L1)

② "동적으로 만든 화면에 초기 상태를 안 지정해서 안 보였다"
   확정 플랜 단일화 직후 .active 누락으로 캘린더 카드가 안 뜰 뻔함
   → 코드만 보지 말고 실제 렌더링 결과를 재현해서 확인할 것 (L1)

③ "방법이 여러 개인데 임의로 하나를 골랐다가 전부 폐기됐다"
   "44일 플랜에 하루 붙이면 되겠네" → 임의로 45일 플랜을 만들었으나
   사용자가 완전히 다른 46일 4회차 확정안으로 대체
   → 방법이 갈리는 지점에서는 옵션을 제시하고 사용자가 고르게 할 것 (0번 절차)
```
